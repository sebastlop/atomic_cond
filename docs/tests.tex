\documentclass[aps, preprint]{revtex4-2}

\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{colortbl}

\begin{document}

\title{Some model tests}

\author{Author Name}
\affiliation{Affiliation}

\date{\today}

\begin{abstract}
Some numerical test about training and testing of cross section fitting 
using neural networks
\end{abstract}

\maketitle

\section{Introduction}
\label{sec:intro}

We use a neural network as universal interpolator to 
fit calculations of total cross sections (TCS) within two theoretical 
models, namely the Continuum-Distorted-Wave with Eikonal-Initial
-State (CDW-EIS) and the Classical Trajectory MonteCarlo (CTMC)
for ionization of hydrogen by bare ion impact. Here we consider
bare projectiles with charges $Z_P$ ranging between 1 and 9 

\section{Methods}
\label{sec:methods}

\subsection{The training set}
The training set consist in a number of $1013$ TCS. We have 
calculated $ 513 $ TCS with the CDW-EIS model and the CTMC model 
as it is specified in Table \ref{Table-TS}. It is important to 
note that we have considered here the semiempiric model of Rudd 
[poner cita] on an equal footing with the theoretical models.

\begin{table}[h!]
\centering
\begin{tabular}{|c|c|c|c|}
\hline
\textbf{Theory} & Number of Impact Energies & 
Impact Energy Range (keV/amu) & Ion charges\\ \hline
\textbf{CDW-EIS} & 37 & 10 - 100000 & 1 - 9\\ \hline
\textbf{CTMC} & 20 & 15 - 2500 & 1 - 9\\ \hline
\textbf{Semi Empiric} & 500 & 10 - 5000 & 1\\ \hline

\end{tabular}
\caption{Number and energy range of TCS calculated for each model.}
\label{Table-TS}
\end{table}

The input variables are, the impact energy,
the projectile charge, and the theory used to obtain the TCS.
Since MLPs are not suitable to handle very tiny numbers nor 
several orders of magnitude, as in the case of this 
training set, we must perform a preprocessing stage that 
include the data compression by taking the decimal logarithm of the 
impact energy and the TCS. These preprocessing allow us to 
avoid gradient vanishing in the backpropagation algorithm 
used to fit the data because using tiny floating point 
values for the energy and the TCS. Moreover, logarithm produce
a compactacion of the several magnitude orders that TCS 
present for the wide impact energies considered here.  
On the other hand, the categorical 
variable corresponding 
to the theory must be casted to a numerical value. We choose 
to expand a three dimensional One Hot Encoding for the three 
theories considered here.

\subsection{The model}

We show in Figure \ref{fig:scheme} a scheme of the model used 
in this work. After the preprocessing stage we have a MLP with a 
5-dimensional input, then the hidden layers and a single output 
corresponding to the $\log_{10}$ of the TCS.

\begin{figure}[htbp]

    \centering
    \includegraphics[width=0.8\textwidth]{./Esquema_ionNN.pdf}
    \caption{Interpolator scheme}
    \label{fig:scheme}
\end{figure}

To find the best architecture of the MLP, as the 
training set is not big enough, we have performed averaging
over a k-fold split over the training set, and the corresponding 
training for models with hidden layers ranging from one to five, 
with different number of units per layer ($2^n$ for $n = 
\{4,...,8\}$).
As it is well known, to introduce non linearity in the MLPs, 
non linear activation functions must be used for each unit 
output. Unlike the usual trend to use the fast-to-compute 
Rectifier Linear Unit activation function (ReLU), in this work we use 
two kind of infinite differentiable functions: the hyperbolic 
tangent, and a gaussian activation. The choice of smooth 
activation functions can be easily justified by the fact that 
TCS are smooth functions of the impact energy, and extreme 
non differentiable oscillating fits caused by ReLU and its 
variations are naturally avoided.

This fitting process is a typical supervised learning. In supervised 
learning some \emph{distance} function between the model prediction $y_{pred}$ 
and the true value $y_{true}$ must be computed. This \emph{distance} 
function, is called the loss function $\mathcal{L}_{\theta}$, where $\theta$ 
refer to the MLP parameters. In this work we explore two loss functions

\begin{align}
    \mathcal{L}_{\theta}^{LCH}(y_{true},y_{pred}) & =  
    \log[\cosh(y_{pred} - y_{true})] \label{Eq-loss-lch}  \\
    \mathcal{L}_{\theta}^{ERR}(y_{true},y_{pred}) & =  
    L_1(y_{pred},y_{true}) + L_2(y_{pred},y_{true}) + Rel(y_{pred},y_{true}) 
    \label{Eq-loss-err}  
\end{align}    

In Eq. (\ref{Eq-loss-err}) three different metrics are added,
\begin{align}
    L_{1}(y_{true},y_{pred}) & =  \frac{1}{N} 
    \sum_{i=1}^{N}{ \left| y_{pred} - y_{true} \right|}
    \label{Eq-l1-mae}  \\
    L_{2}(y_{true},y_{pred}) & =  \frac{1}{N} 
    \sum_{i=1}^{N}{ \left( y_{pred} - y_{true} \right) ^2}
    \label{Eq-l2-mse}  \\  
    Rel(y_{true},y_{pred}) & =  \frac{1}{N} 
    \sum_{i=1}^{N}{ \left| \frac{y_{pred} - y_{true}}{y_{true}} \right|}.
    \label{Eq-rel-rel}
\end{align}

Usually, $L_1$, $L_2$, and $Rel$ are named as the mean 
absolute error (MAE), the mean squared error (MSE) and 
the relative error, respectively. We combine these three 
measures in order to guide the optimization algorithm 
for the different ranges in the $\log_{10}(TCS)$. 
Meanwhile, on one hand MSE ponderates differences where 
target values are higher, the MAE does it at intermediate 
regions. On the other hand, $Rel$ runs over whole range 
on equal footing.

We are dealing with analytic activation functions, 
thus we can use the gradient descent algorithm 
or any variant of it. In this work we use the Adam 
algorithm with an initial learning rate of $lr=1\times10^{-4}$
to \emph{backpropagate} the model and update the parameters.

In Fig. \ref{fig:training_losses} we show the losses evolution during 
the training process.
\begin{figure}[htbp]

    \centering
    \includegraphics[width=0.8\textwidth]{./metrics_loss.png}
    \caption{Averaged in 5-folded loss functions calculated over 
    the validation set as function of the epoch. The number of 
    hidden layers is two and 
    Left column: training loss for 
    $\mathcal{L}_\theta^{LCH}$ as the Panels (a-c) loss function for the training set.
    Panels(d-f) loss function for the validation set. Each row 
    correspond to 32 (panels (a) and (d)), 64 (panels (b) and (e)) 
    and 128 (panels (c) and (f)) hidden units per layer respectively.}
    \label{fig:training_losses}
\end{figure}

We show in Fig. (\ref{fig:training_losses}) 

We use PyTorch framework to optimize the fitting of a multilayer perceptron (MLP) over a set of pre-calculated
ionization TCS.

The architecture of the MLP has an input of the projectile impact 
energy, the charge of the projectile and the theory used

\begin{itemize}
    \item Impact energy: we use decimal logarithm to flat the data in 
    usable ranges $\approx 10$
    \item Projectile charge: casted to float
    \item Theory: we one-hot encode the theory used (CTMC, CDW-EIS and 
    semi-empirical formula for proton impact by Rudd et al.)
\end{itemize}


\begin{itemize}
    \item As the number hidden units increase, the lower loss reached.
    \item The gaussian activation function has better performance
\end{itemize}

VA CON 

We noted that 
increasing the number of layers become meaningless after a total 
of three layers. On the other hand the use of different number 
of units in the different layers did not improve the fitting, 
for these reasons we choose to employ a MLP with three hidden 
layers and present the study of the training performance for 
the case with 32, 64 and 128 units per layer.

\subsection{Training process}
For the training process we use the Adam algorithm, which is based 
in gradient descent algorithm. 
To compute distances we use 

\section{Results}
\label{sec:results}

\subsection{Training process}


In Fig. \ref{fig:main_metrics} we show the evolution during 
the training process for several global metrics that compute the error
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{./metrics_main.png}
    \caption{Metrics over validation set for different network 
    architectures. Left column (a, c, and e) metrics with $\tanh$ 
    activation function. Right column (b-d-f) gaussian activation 
    function. Each row correspond to a different metric over the 
    $\log_{10}$ of the TCS.}
    \label{fig:main_metrics}
\end{figure}

\begin{itemize}
    \item As the number hidden units increase, MAE and MRE seems to 
    converge for gaussian activation.
    \item $\tanh$ present some overfitting. 
    \item The gaussian activation function has slightly better 
    performance
\end{itemize}

In Fig. \ref{fig:max_metrics} we show the evolution during 
the training process for several maximum of the former metrics 
to understand the deviations over each model

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{./metrics_max.png}
    \caption{Maximum values of Metrics over validation set 
    for different network architectures. 
    Left column (a, c) metrics with $\tanh$ 
    activation function. Right column (b-d) gaussian activation 
    function. Each row correspond to a different metric over the 
    $\log_{10}$ of the TCS.}
    \label{fig:main_metrics}
\end{figure}

\begin{itemize}
    \item This plot shows the power of gaussian activation.

\end{itemize}

\subsection{Evaluation stage}

We perform some evaluations over the whole TCS training set 
with the best models (128 hidden units) to understand the different 
errors in $cm^2$. Even when we have determined that gaussian activation 
has better performance, we show results for both activation types


\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{../performance_tanh.png}
    \caption{Prediction of the MLP for CDW-EIS (left column) and 
    CTMC (right column) as a function of the projectile impact energy.
    Each row correspont to a different projectile charge from $ Z_P=1$ 
    to $Z_P = 9$. Activation function: $\tanh$ }
    \label{fig:performance_tanh}
\end{figure}


\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{../performance_gauss.png}
    \caption{Prediction of the MLP for CDW-EIS (left column) and 
    CTMC (right column) as a function of the projectile impact energy.
    Each row correspont to a different projectile charge from $ Z_P=1$ 
    to $Z_P = 9$. Activation function: gaussian.}
    \label{fig:performance_gauss}
\end{figure}


\begin{itemize}
    \item This plot shows the power of gaussian activation.

\end{itemize}

\section{Discussion}
\label{sec:discussion}

\begin{itemize}
    \item WE NEED TO DO THE EXPERIMENT WITH ZP HOLES AND ENERGY 
    EXTRAPOLATIONS
    \item WE NEED TO DEFINE RANGES OF INPUT ENERGIES IN THE 
    INPUT PIPELINE
    \item SEBASTIAN HAS TO PLOT THE SEMIEMPIRIC FIT!!!!
    \item MISSING THE BENCHMARKING AGAINST THE CUBIC INTERPOLATOR. 
    ASK TO MICHELE IN DEEP.
\end{itemize}

\section{Conclusion}
\label{sec:conclusion}

Summarize your conclusions and future work.

\begin{thebibliography}{99}

\bibitem{ref1} Author, ``Title,'' \textit{Journal}, vol. X, pp. XX-XX, Year.

\end{thebibliography}

\end{document}